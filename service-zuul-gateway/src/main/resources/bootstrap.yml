spring:
  application:
    name: service-zuul-gateway
  cloud:
    config:
      name: service-zuul-gateway-config   #文件前缀名称
      profile: dev                  #服务环境名称
      uri: http://localhost:8888